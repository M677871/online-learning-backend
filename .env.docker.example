# Backend
NODE_ENV=development
PORT=3000

# MariaDB
# Keep DB_NAME aligned with database/schema.sql unless you also update that SQL file.
DB_HOST=db
DB_PORT=3306
DB_USER=root
DB_PASS=localdev-db-password
DB_NAME=csis_228_project
DB_POOL_LIMIT=10

# JWT
JWT_SECRET=localdev-change-this-jwt-secret
JWT_EXPIRES_IN=1h
JWT_ISSUER=online-learning-api

# Bcrypt
BCRYPT_SALT_ROUNDS=10
