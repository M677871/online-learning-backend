<nav class="topbar">
  <a href="/dashboard" class="brand">
    <span class="brand-badge">LO</span>
    <span>LearnOnline</span>
  </a>

  <div class="nav-links">
    <a href="/dashboard" class="nav-link <%= active === 'dashboard' ? 'active' : '' %>">Dashboard</a>
    <a href="/courses" class="nav-link <%= active === 'courses' ? 'active' : '' %>">Courses</a>
    <a href="/students" class="nav-link <%= active === 'students' ? 'active' : '' %>">Students</a>
    <a href="/instructors" class="nav-link <%= active === 'instructors' ? 'active' : '' %>">Instructors</a>
    <a href="/quizzes" class="nav-link <%= active === 'quizzes' ? 'active' : '' %>">Quizzes</a>
  </div>

  <div class="nav-links">
    <button class="theme-toggle" type="button" data-theme-toggle aria-label="Toggle theme">
      <i class="bi bi-moon"></i>
    </button>
    <% if (user) { %>
      <span class="chip"><%= user.userType %></span>
      <div class="dropdown">
        <button class="btn btn-ghost dropdown-toggle" data-bs-toggle="dropdown">
          <i class="bi bi-person-circle"></i> <%= user.email %>
        </button>
        <ul class="dropdown-menu dropdown-menu-end">
          <li><a class="dropdown-item" href="/changePassword">Change Password</a></li>
          <li><a class="dropdown-item text-danger" href="/logout">Logout</a></li>
        </ul>
      </div>
    <% } else { %>
      <a class="btn btn-ghost" href="/login">Login</a>
      <a class="btn btn-primary" href="/signup">Sign Up</a>
    <% } %>
  </div>
</nav>
